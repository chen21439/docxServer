spring.application.name=docxServer

# 服务端口
server.port=9199

# 文件上传大小限制
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# 文档存储基础目录 (默认值，可被环境配置覆盖)
docx.storage.base-path=/data/docx_server
